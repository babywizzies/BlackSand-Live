export interface Registration {
    id: string,
    collection: string,
    discord_handle: string,
    treats: string[],
    wallet?: string,
    pony_name?: string,
    signature?: string,
    baby?: string,
    team?: string,
}